modules = ["nodejs-18"]

# Main run command for development
run = ["npm", "run", "dev"]

[deployment]
run = ["npm", "run", "start:replit"]

[[ports]]
localPort = 5000
externalPort = 80

[env]
NODE_ENV = "development"
